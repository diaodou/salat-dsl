# salat-dsl

salat-dsl is a mongodb query dsl library, which is based on salat.

Salat is a bi-directional Scala case class serialization library that leverages
MongoDB's `DBObject` (which uses BSON underneath) as its target format.

# Examples

[TestDao.scala](https://github.com/diaodou/salat-dsl/blob/master/src/main/scala/com/poseidon/dsl/TestDao.scala)